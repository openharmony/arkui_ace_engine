# 老框架组件隔离方案指导（Legacy Framework Component Isolation Guide）

> 适用范围：`OpenHarmony/foundation/arkui/ace_engine`
> 状态：基准方案（v1.10）。导出符号收敛，以及面向外部或独立版本演进的系统性边界协议设计属后续专题；复用既有 Model 抽象的动态加载模式、级联迁移的同 SO 直连模式、高接口密度组件的内部 modifier + accessor 模式属于本方案范围。
> 原则遵循 [CLAUDE.md](../CLAUDE.md) §4：代码优先、证据优先、不臆造。

---

## 0. 文档目的与范围

### 0.1 目的
指导把 `frameworks/core/components` 下的**老框架（V1/DOM 式）组件实现**，从主引擎库中隔离，迁移到 `frameworks/compatible`，独立编译为共享库 `libace_compatible_components.z.so`，主引擎运行时按需动态加载。

### 0.2 在范围内
- 老框架组件的搬迁方法、标准流程、检查项
- 已有隔离机制（`ComponentLoader` + 动态加载）的说明与复用
- 已完成 components_ng 组件化、但 V1 `ModelImpl` 和老框架实现仍留在 core 时的补充隔离模式
- 组件分级、迁移顺序、风险红线

### 0.3 不在范围内（后续专题）
- 导出符号（`libace.map`）的收敛
- 面向外部接口、独立升级或独立卸载场景的系统性边界协议设计（版本协商、稳定 ABI、基类下沉等）

> 当前按**主库符号直接导出**方式实施（compatible 链接 core，所需符号经 `libace.map` 导出）；3.7 所述 accessor 仅是同次构建、同步发布的内部边界模式，不承担独立 ABI 演进能力。上述外部化、版本化优化另行讨论。

---

## 1. 背景与现状

### 1.1 架构目标
**【架构解耦】**：把老框架实现静态剥离出主引擎库，按需 `dlopen`，使主引擎（新框架 `components_ng`）不再静态携带老框架代码。这是若干 `【架构解耦】` 提交的统一目标。

### 1.2 现状量化（实测）
| 指标 | 实测值 | 来源 |
|---|---|---|
| `libace.map` 符号行数 | ~5152 | [build/libace.map](../build/libace.map)（5160 行） |
| `ACE_EXPORT` 注解 | 2673 处 | 头文件统计 |
| `ACE_FORCE_EXPORT` 注解 | 2004 处 | 头文件统计 |
| 跨 .so 导出 vtable | 193 | map 内 |
| 跨 .so 导出 VTT | 148 | map 内 |
| 跨 .so 数据符号（`DOM_` 常量） | 19 | map 内 |
| `core/components` 剩余目录 | 105（92 仍自带 `BUILD.gn`） | |
| `compatible/components` 已迁移目录 | 30 | |

> 当前采用"主库符号直接导出"方式：导出面规模、含内部实现类与 RTTI（vtable/VTT）为此方式的客观表现。导出收敛，以及面向外部或独立版本演进的系统性边界协议设计属后续优化专题；同构建内部 accessor 模式见 3.7。

### 1.3 已验证可行的机制
chart（[提交 46648ae](../frameworks/compatible/components/chart)）、badge、list、swiper、image-animator 等已迁移或部分迁移，证明 `ComponentLoader` + `DynamicModuleHelper` + 单 C 入口的隔离机制可用。

### 1.4 core/components 的两条消费路径
`core/components/` 老框架组件同时支撑**两个运行场景**，迁移时两条路径都必须保持可用：

1. **类 Web 范式（DOMxx 入口）**：经 `frameworks/bridge/js_frontend` + `frameworks/bridge/common/dom/dom_<x>`（如 `DOMChart`、`DOMBadge`）解析 DOM，构建 Component/Element/Render 树。
2. **声明式范式 + 老管线**：`declarative_frontend`（及 `cj_frontend` / `plugin_frontend`）在 `Container::IsCurrentUseNewPipeline() == false` 时走老管线（`pipeline`，非 `pipeline_ng`），同样构造 `core/components` 老组件，而非 components_ng 的 FrameNode。

> 关键事实：`useNewPipeline_` 默认 `false`（[container.h:787](../frameworks/core/common/container.h#L787)），新管线为 opt-in。判定函数 `IsCurrentUseNewPipeline()`（[container.h:393](../frameworks/core/common/container.h#L393)）在 [declarative_frontend.cpp:598](../frameworks/bridge/declarative_frontend/declarative_frontend.cpp#L598)、[frontend_delegate_declarative.cpp:206](../frameworks/bridge/declarative_frontend/frontend_delegate_declarative.cpp#L206) 及 js_frontend / cj_frontend / plugin_frontend 均有分支判定。

**对迁移的影响**：
- DOM 路径由 `ComponentLoader::CreateDomNode`（`DynamicModuleHelper::GetLoaderByName`）承接。
- 声明式老管线路径通过 V1 Model（`ComponentLoader::CreateModel`）及 jsview 老管线适配接入。
- **迁移一个组件时，两条路径都要验证**（类 Web DOM 场景 + 声明式 `IsCurrentUseNewPipeline()==false` 老管线场景），不可只测其一。

---

## 2. 目标架构

### 2.1 目录归宿
| 内容 | 归宿 | 说明 |
|---|---|---|
| 新框架组件（`FrameNode`+`Pattern`+`Modifier`） | `frameworks/core/components_ng` | 新开发首选，不参与本迁移 |
| 老框架组件实现（Component/Element/Render/DOM） | `frameworks/compatible/components/<x>` | **本方案迁移目标** |
| 公共值类型/工具（Color/Dimension/RefPtr 等） | `frameworks/base`、`frameworks/core/components/common` | 双方共用，**不下沉到 compatible** |
| 渲染管线、Pipeline 基类、Theme/Font/Animation 等核心服务 | `frameworks/core` | 保留在 core，作为 compatible 的依赖方 |

### 2.2 迁移"稳态"形态
一个组件完成迁移后，`frameworks/core/components/<x>/` 应仅保留：
- 主题资源（`*_theme.h`）
- 必要时保留反向桥（`<x>_compatible_modifier_helper.{h,cpp}`，仅用于公共 Model/DOM 接口无法覆盖的组件特有调用）

主体实现（component/element/render/dom/model）全部进入 `frameworks/compatible/components/<x>/`。

> 稳态不要求每个组件都存在 helper。若已有公共 `Model` 抽象且 loader 可通过 `CreateModel()` 返回 compatible 中的 `ModelImpl`，core 可只保留 theme；DataPanel 即为此形态。若仍有公共接口无法表达的反向调用，再保留 helper/custom modifier。实测参照：List/Swiper 为 theme + helper；Rating/DataPanel 的老管线 Model 走 `CreateModel()`。

---

## 3. 隔离机制：ComponentLoader 模式

### 3.1 四层结构
| 层 | 文件 | 职责 |
|---|---|---|
| ① 抽象工厂 | [component_loader.h](../frameworks/compatible/components/component_loader.h) | 纯虚接口：`CreateDomNode/CreateModel/CreateInspectorElement/CreateDeclaration/GetCustomModifier/...` |
| ② 单组件 Loader | `<x>_loader.{h,cpp}` | 子类实现各虚函数 |
| ③ C 入口 + 注册表 | [component_loader.cpp](../frameworks/compatible/components/component_loader.cpp) | `extern "C" OHOS_ACE_Compatible_GetLoader(name)` + `unordered_map<name, factory>` |
| ④ 核心侧动态绑定 | [dynamic_module_helper.cpp](../adapter/ohos/osal/dynamic_module_helper.cpp) | `dlopen(libace_compatible_components.z.so)` → `dlsym(GetLoader)` → 缓存 → 返回 `unique_ptr<ComponentLoader>` |

### 3.2 加载流程
1. 核心侧任意调用点请求 `DynamicModuleHelper::GetInstance().GetLoaderByName("<x>")`。
2. 首次调用时 [dynamic_module_helper.cpp](../adapter/ohos/osal/dynamic_module_helper.cpp) `dlopen` 兼容库，`dlsym` 取 `OHOS_ACE_Compatible_GetLoader`，缓存函数指针。
3. 该 C 入口在 [component_loader.cpp](../frameworks/compatible/components/component_loader.cpp) 内经静态 `sLoaderMap` 按名分发，返回对应 `ComponentLoader*`。

> 注意区分两条动态加载通道：`GetLoaderByName` → `libace_compatible_components.z.so`（老框架 DOM 组件）；`GetDynamicModule` → `libarkui_<x>.z.so`（`components_ng` 动态模块）。本方案仅涉及前者。

### 3.3 反向调用（compatible → core 的特有方法）
跨 .so 后，核心侧**不能 `DynamicCast` 到 compatible 的具体 DOM 类型**。对组件特有方法，采用**函数指针结构体**：
- compatible 侧定义 `ArkUI<X>ModifierCompatible`（含若干函数指针字段，如 `set<X>Datasets`）。
- `ComponentLoader::GetCustomModifier()` 返回该结构体指针。
- 核心侧（如 [js_command.cpp](../frameworks/bridge/js_frontend/js_command.cpp)）经该结构体回调，替代原 `DynamicCast` 直调。

对接口数量少、粗粒度、边界稳定的组件，优先使用上述标准 modifier 平铺模式。对 Web 这类接口数量多、调用高内聚、且需要在使用时读取当前状态的内部组件，可按 3.7 采用 **modifier + accessor** 模式：modifier 仅负责能力发现与 concrete cast，高密度业务调用由 core 公共抽象 accessor 承载。

### 3.4 Declaration 动态创建（DOM 解析路径）
DOMNode 构造时会通过 [declaration_creator_manager.cpp](../frameworks/core/components/declaration/common/declaration_creator_manager.cpp) 创建 Declaration。其流程是：
1. 先查 core 内静态 `declarationCreators` 表（当前只保留 button/text/span/web/xcomponent 等未迁或基础项）。
2. 静态表未命中时，调用 `DynamicModuleHelper::GetInstance().GetLoaderByName(tag.c_str())`。
3. 若 loader 存在，则调用 `loader->CreateDeclaration()`，由 compatible 侧返回组件专属 Declaration。

因此组件迁入 compatible 时，**组件专属 Declaration 不应保留在 core 静态表中**。正确模式参照：
- [badge_loader.cpp](../frameworks/compatible/components/badge/badge_loader.cpp)：`CreateDeclaration()` 返回 `BadgeDeclaration`；
- [qrcode_loader.cpp](../frameworks/compatible/components/qrcode/qrcode_loader.cpp)：返回 `QrcodeDeclaration`；
- [piece_loader.cpp](../frameworks/compatible/components/piece/piece_loader.cpp)：返回 `PieceDeclaration`；
- [svg_loader.cpp](../frameworks/compatible/components/svg/svg_loader.cpp)：各 SVG tag 返回对应 Declaration。

配套要求：
- 将 `<x>_declaration.{h,cpp}` 迁入 `frameworks/compatible/components/<x>/`（若该组件有专属 Declaration）。
- 从 `DeclarationCreatorManager` 的静态表、include 中删除该 Declaration，确保按 tag 走 loader fallback。
- 若 `DeclarationConstants` 中仍有该组件专属默认 attr/style/event/method（例如 `DEFAULT_<X>_ATTR`），应随 Declaration 下沉到 compatible 的 `<x>_declaration.cpp` 文件级常量；core 的 `DeclarationConstants` 只保留公共/未迁组件默认项。

### 3.5 已组件化组件的老框架 Model 隔离（Rating / DataPanel 模式）

#### 3.5.1 适用场景

部分组件已经完成 components_ng 组件化和动态 modifier 接线，但旧管线分支仍在 core 静态构造 V1 `ModelImpl`。典型特征如下：

1. `frameworks/core/components_ng/pattern/<x>/bridge/<x>_dynamic_modifier.cpp` 已存在，并按 `Container::IsCurrentUseNewPipeline()` 区分 NG 与旧管线实现。
2. core 已有公共抽象接口 `<X>Model`，旧管线所需操作均可通过该虚接口表达。
3. `<X>ModelImpl` 仍位于 core，并直接构造 `Component/Element/Render` 老框架具体类型。
4. 组件老框架具体实现正准备或已经迁入 `frameworks/compatible/components/<x>/`。

此时 `<X>ModelImpl` 也是老框架实现的一部分，必须随 Component/Element/Render 一起迁入 compatible。仅迁 concrete Component、却把 ModelImpl 留在 core，再新增 custom modifier/helper 转发，是不必要的双重边界。

#### 3.5.2 标准加载链

```text
<x>_dynamic_modifier.cpp（core，旧管线分支）
    │ 只持有 <X>Model* 公共接口
    ▼
DynamicModuleHelper::GetLoaderByName("<x>")
    ▼
<X>Loader::CreateModel()
    ▼
new Framework::<X>ModelImpl()（compatible）
    ▼
直接构造/配置 compatible 内的 V1 Component/Element/Render
```

core dynamic modifier 参考 Rating 的写法：

```cpp
#ifndef CROSS_PLATFORM
XModel* GetXModelImpl()
{
    static auto loader = DynamicModuleHelper::GetInstance().GetLoaderByName("<x>");
    static XModel* instance = loader ? reinterpret_cast<XModel*>(loader->CreateModel()) : nullptr;
    return instance;
}
#endif
```

其中 `XModel`/`GetXModelImpl` 是占位写法，落地时替换为真实类型和函数名。loader 与 Model 指针必须作为函数内 `static` 缓存，避免每次 modifier 调用重复创建；保持原有 `#ifndef CROSS_PLATFORM` 边界，不改 NG 新管线分支。`CreateModel()` 返回对象的生命周期沿用 Rating 现有进程级缓存模式，core 不单独释放该 raw pointer。

compatible loader：

```cpp
void* <X>Loader::CreateModel()
{
    return new Framework::<X>ModelImpl();
}
```

已验证参考：

- Rating core 动态获取：[rating_dynamic_modifier.cpp](frameworks/core/components_ng/pattern/rating/bridge/rating_dynamic_modifier.cpp)；compatible 创建 Model：[rating_loader.cpp](frameworks/compatible/components/rating/rating_loader.cpp)、[rating_model_impl.cpp](frameworks/compatible/components/rating/rating_model_impl.cpp)。
- DataPanel core 动态获取：[data_panel_dynamic_modifier.cpp](frameworks/core/components_ng/pattern/data_panel/bridge/data_panel_dynamic_modifier.cpp)；compatible 创建 Model：[data_panel_loader.cpp](frameworks/compatible/components/data_panel/data_panel_loader.cpp)、[data_panel_model_impl.cpp](frameworks/compatible/components/data_panel/data_panel_model_impl.cpp)。

#### 3.5.3 决策规则

| 判断 | 处理模式 |
|---|---|
| 已有公共 `<X>Model`，且旧管线调用都在其虚接口内 | **优先使用 `CreateModel()`**：整体迁移 `<X>ModelImpl`，core 只持有 `<X>Model*` |
| core 仍需调用 Model 接口之外的组件特有方法 | 仅为这些额外方法设计 `GetCustomModifier()` 函数指针；不要把 Model 已覆盖的方法重复放入 modifier |
| 组件没有旧管线 Model | `CreateModel()` 返回 `nullptr`，按 DOM/Inspector/Declaration 实际需求实现其他 loader 接口 |
| core 仍需识别 compatible 具体 DOM/Component 类型 | 改为 tag/公共基类判定，并通过 custom modifier 完成特有操作；禁止跨 `.so` concrete cast |

#### 3.5.4 文件与构建改线

1. 将 `<x>_model_impl.{h,cpp}` 从 `components_ng/pattern/<x>/bridge/` 或其他 core 路径迁入 `compatible/components/<x>/`，改写 include 和头文件守卫。
2. compatible `ModelImpl` 恢复/保留对 V1 concrete Component 的直接构造、cast、setter 和 ViewStackProcessor 压栈逻辑；这些调用已与 concrete 实现在同一 `.so` 内。
3. core `<x>_dynamic_modifier.cpp` 删除 ModelImpl 头，只 include 公共 `<x>_model.h`，返回类型从具体 `<X>ModelImpl*` 改为 `<X>Model*`。
4. `<X>Loader::CreateModel()` 返回新建的 compatible ModelImpl；loader key 必须与 dynamic modifier 的 `GetLoaderByName("<x>")` 完全一致。
5. 从 components_ng `<x>/BUILD.gn` 删除旧 ModelImpl source，在 `frameworks/compatible/BUILD.gn` 增加新 source；若 core 组件目录只剩 theme，删除空置 BUILD 子目标及 core 对它的依赖。
6. 删除因过渡方案产生且已无消费者的 modifier/helper，并同步删除仅为该 helper 增加的 `libace.map` 导出。

#### 3.5.5 禁止的混合形态

- core dynamic modifier include compatible 的 `<x>_model_impl.h` 或保存具体 `<X>ModelImpl*`。
- `<X>ModelImpl` 同时在 core 与 compatible BUILD 中编译。
- 公共 Model 已覆盖全部操作，仍额外保留一套 `ArkUI<X>ModifierCompatible` 转发相同方法。
- `CreateModel()` 已可返回 ModelImpl，却仍让 loader 返回 `nullptr`，由 core helper 绕行 `GetCustomModifier()` 创建组件。
- 为已删除的 helper/custom modifier 保留无真实引用的 `ACE_FORCE_EXPORT` 或 `libace.map` 条目。

#### 3.5.6 专项验证

除通用迁移检查外，必须增加 ModelImpl 归属检查：

```bash
# core 不得再引用具体 ModelImpl 或旧路径
rg -n '<X>ModelImpl|<x>_model_impl.h' frameworks/core adapter interfaces test

# dynamic modifier 必须通过 loader CreateModel 获取公共 Model 指针
rg -n 'GetLoaderByName\("<x>"\)|loader->CreateModel\(\)' \
  frameworks/core/components_ng/pattern/<x>/bridge/<x>_dynamic_modifier.cpp

# 符号应只存在于 compatible components .so
nm -C --defined-only out/rk3568/lib.unstripped/arkui/ace_engine/libace_compatible.z.so | rg '<X>ModelImpl'
nm -C --defined-only out/rk3568/lib.unstripped/arkui/ace_engine/libace_compatible_components.z.so | rg '<X>ModelImpl'
```

期望结果：主 `.so` 为 0，components `.so` 大于 0；同时验证新管线分支仍直接使用 ModelNG，旧管线分支可经 loader 创建 ModelImpl。DataPanel 实测为主 `.so` 0、components `.so` 14。

### 3.6 同 SO 直连 vs 跨 SO 走 helper（导出面自然收敛模式）

#### 3.6.1 背景与问题
3.3 节的函数指针 `Custom Modifier / Helper`（如 `ListCompatibleModifierHelper::GetListItemCompatibleModifier()`）本质是**为跨 SO 调用**（core → compat_components）设计的门面。其**核心侧实现**（`core/components/<x>/<x>_compatible_modifier_helper.{h,cpp}`）内部缓存 loader `dlopen` 后注册的函数指针，并暴露给 core 的多个消费者复用。

问题出现在**级联迁移**场景：假设 helper 原本服务 core→compat 的 N 个 caller，其中若干 caller 本身也开始被迁入 compatible（例如 `IndexerComponent` 是 helper 的一个 caller，迁移后自己也进了 `libace_compatible_components.z.so`）。此时若不改代码，caller 会**在同一 SO 内绕出去再回来**：
```
IndexerComponent  (compat_components.so)
        │  ① 跨 SO 调用（本次迁移的产物）
        ▼
ListCompatibleModifierHelper::GetListItemCompatibleModifier()  (libace_compatible.so)
        │  返回缓存的 fn-ptr struct 指针
        ▼
ArkUIListItemCompatibleModifier* instance  (地址在 compat_components.so)
        │  ② 通过 fn-ptr 跳回来
        ▼
modifier->getListItemComponent(...)  (compat_components.so)
```
两次跨 SO 跳转 + 一次 fn-ptr indirection，还不得不给 helper 方法加 `ACE_FORCE_EXPORT` + `libace.map` 显式导出条目——**只是为了让本可以同 SO 直连的 caller 借道 helper**。

#### 3.6.2 规则
在**同 SO 内**（compat_components → compat_components）调用组件族内部的 modifier / factory，**必须走直连**，不通过 core 侧 helper 门面。核心侧 helper 保留原状，只服务**真正的跨 SO caller**（即仍在 core / libace_compatible.so 内的消费者）。

具体：
- **接口层**（`compatible/components/<mod>/<mod>_compatible_modifier.h`）里公开访问器声明：
  ```cpp
  namespace OHOS::Ace::<Mod>CompatibleModifier {
  const ArkUI<Mod>CompatibleModifier* Get<Mod>CompatibleModifier();
  } // namespace
  ```
  这是**接口契约**（fn-ptr struct + 访问器）；同 SO caller 只依赖接口，不依赖 core 侧 helper 具体实现。
- **同 SO 的 caller**（新迁入 compatible 的组件）改为 `#include "compatible/components/<mod>/<mod>_compatible_modifier.h"`，直接调 `<Mod>CompatibleModifier::Get<Mod>CompatibleModifier()`。
- **core 侧 helper**（`ListCompatibleModifierHelper` 等）**不变**，继续为 core 侧 caller 服务；但如果本次迁移之后**helper 已无跨 SO caller**，同步回退它此前为该 caller 加的 `ACE_FORCE_EXPORT` 与 `libace.map` 条目——导出面会由链接器自动降级为 local（`nm -C` 中的 `t` 而非 `T`）。

#### 3.6.3 判定流程（每次组件迁移之后必查）
每完成一个组件迁移（例如把 `<X>Component` 从 core 挪到 compat_components），对这个组件依赖的每个 `Xxx::GetXxxCompatibleModifier()` / `Xxx::CreateXxx()` 类跨 SO 门面执行以下检查：

1. **grep 迁移后调用点**：搜 `frameworks/compatible/` 下是否仍有对 core 侧 helper 的引用：
   ```bash
   grep -rn "ListCompatibleModifierHelper::" frameworks/compatible/
   ```
2. **如果 compatible 侧命中** → 属于"同 SO 借道"，改为直连（改 include + 换命名空间调用），并**同步补足**接口层 header 的访问器声明（若原来只在 .cpp 里 forward-decl）。
3. **改完后再 grep 全库** helper 方法：
   ```bash
   grep -rn "<Helper>::<Method>" frameworks/ adapter/ interfaces/ test/
   ```
   - 若命中全部落在 `libace_compatible.so`（`frameworks/core/`、`frameworks/bridge/`、`adapter/ohos/`）→ **helper 已无跨 SO caller**，回退它此前为该迁移新增的 `ACE_FORCE_EXPORT` 与 `libace.map` 条目。
   - 若仍有 compat_components 侧命中 → helper 仍需保留导出，继续维护。
4. **核实链接器已物理收窄**：
   ```bash
   nm -C --defined-only out/rk3568/lib.unstripped/arkui/ace_engine/libace_compatible.z.so \
     | grep "<Helper>::<Method>"
   ```
   目标：符号类型显示为 `t`（local，仅本 SO 可见）而非 `T`（global）。一旦为 `t`，未来任何 compat 侧代码想借道会得到明确的 SOLINK undefined 错误——**约定升级为链接器强制**。

#### 3.6.4 参考实现
- **IndexerComponent 阶段 2 refactor**（2026-07-18）：
  - 迁移把 `IndexerComponent` 从 core 挪到 `compat_components`，其内部两处调用 `ListCompatibleModifierHelper::GetListItemCompatibleModifier()`。
  - 初始阶段 build-fix 为让链接过，给 helper 加了 `ACE_FORCE_EXPORT` + `libace.map` 条目，但 IndexerComponent 是**唯一**的 compat 侧 caller。
  - 收口阶段按 3.6.3 判定流程回滚：
    - 给 [list_item_compatible_modifier.h](../frameworks/compatible/components/list/list_item_compatible_modifier.h) 追加公开访问器 `ListItemCompatibleModifier::GetListItemCompatibleModifier()` 声明。
    - [indexer_component.cpp](../frameworks/compatible/components/indexer/indexer_component.cpp) 换 include + 换调用命名空间。
    - [list_compatible_modifier_helper.h](../frameworks/core/components/list/list_compatible_modifier_helper.h) 回退 `ACE_FORCE_EXPORT` 与 `#include "base/utils/macros.h"`。
    - [libace.map](../build/libace.map) 移除 `ListCompatibleModifierHelper::GetListItemCompatibleModifier`。
    - `nm -C` 核实符号从 `T` 降级为 `t`。
  - 完整过程见 [docs/indexer_list_split_plan_CN.md 二·十节](../docs/indexer_list_split_plan_CN.md)。

#### 3.6.5 与 3.3 的关系（不冲突，是互补）
| 场景 | 依据 | 出口 |
|---|---|---|
| core → compat_components（真跨 SO） | 3.3 fn-ptr modifier + core 侧 helper 门面（`GetLoaderByName` 或 `<X>CompatibleModifierHelper`）| 走 helper，helper 方法带 `ACE_FORCE_EXPORT` + `libace.map` 导出 |
| compat_components → compat_components（同 SO，级联迁移产物）| **3.6 直连** | 走公开接口层访问器（`<Mod>CompatibleModifier::GetXxx`）；helper 不介入 |

不重叠：一个具体调用点只走一条路。如果搞混（compat 侧借道 helper），会拖慢运行时又扩大导出面；如果 core 侧改走直连，会因跨 SO undefined symbol 直接编译失败——两侧的错误都能被机制自动暴露。

### 3.7 高接口密度组件的 modifier + accessor 模式

#### 3.7.1 适用背景

标准 modifier 平铺模式适合少量、粗粒度调用。当一个组件存在数十个高内聚 getter / event / action，且 core 侧需要在不同时机反复读取其当前状态时，将每个方法平铺为 modifier 字段会带来以下问题：

- 每增加一个能力，都需同步修改函数指针字段、compatible 包装函数、静态实例初始化和 core 调用代码。
- 细粒度调用每次都需重复 `DynamicCast` 到 concrete 类型。
- 为减少接口而在初始化阶段复制可变属性，会把原有的“使用时读取当前状态”改成“一次性快照”，存在数据状态不一致风险。

满足以下前提时，可使用 modifier + accessor 作为标准 modifier 的受控补充：

1. `libace_compatible.z.so` 与 `libace_compatible_components.z.so` 由同一次构建产生，使用同一源码、编译器与 C++ ABI。
2. 两个 .so 同步发布，不支持单独替换或独立版本升级。
3. accessor 只是 ace_engine 内部边界，不属于 `interfaces/native` / `interfaces/napi` 公共 API/ABI。
4. compatible 组件库在 accessor 使用期间不会被卸载。

#### 3.7.2 标准结构

accessor 模式不替代 `ComponentLoader` / modifier，而是缩小 modifier 的职责：

```cpp
// core 公共抽象：不 include compatible concrete header
class XComponentAccessor {
public:
    virtual ~XComponentAccessor() = default;
    virtual std::string GetCurrentValue() const = 0;
    virtual bool DispatchAction(const BaseEventInfo* info) = 0;
};

struct XAccessorHolder {
    RefPtr<Component> component;       // 保活 concrete 对象
    XComponentAccessor* accessor = nullptr;

    explicit operator bool() const { return accessor != nullptr; }
    XComponentAccessor* operator->() const { return accessor; }
};

// loader GetCustomModifier() 返回的小型边界表
struct ArkUIXModifierCompatible {
    bool (*isXComponent)(const RefPtr<Component>& component);
    XComponentAccessor* (*getAccessor)(const RefPtr<Component>& component);
};
```

compatible 侧的 concrete 类实现 accessor；`getAccessor` 在 compatible .so 内执行 `DynamicCast<XComponent>`，并返回经多继承指针调整后的 accessor 指针。core 侧只保存 `Component` / `RenderNode` 基类强引用和 accessor 指针，不识别 compatible concrete 类型。

对 Component 状态与 Render 状态同时存在的组件，应按职责拆分为 `XComponentAccessor` 与 `RenderXAccessor`，分别由 `RefPtr<Component>` 和 `RefPtr<RenderNode>` 保活，不把两类生命周期混在同一指针中。

#### 3.7.3 决策规则

| 场景 | 推荐模式 |
|---|---|
| 已有公共 `<X>Model` 完整覆盖旧管线能力 | `CreateModel()` + 公共 Model，不重复设计 modifier/accessor |
| 少量、粗粒度、边界稳定的特有调用 | 标准函数指针 modifier |
| 接口数量多、调用高内聚、需要实时读取 concrete 当前状态 | 小型 modifier + core 公共 accessor |
| 接口可独立版本升级、.so 可单独替换/卸载，或属于外部接口 | 仅使用显式函数指针 modifier，禁止 accessor |
| 原逻辑在初始化后确实不再变化的值 | 经代码验证后可按值传递；未验证不可做快照 |

#### 3.7.4 强制约束

- **抽象归属**：accessor 接口定义放在 core 公共层，签名只使用 core/共享类型；不得 include 或暴露 compatible concrete 类型。
- **cast 位置**：`DynamicCast<XComponent>` / `DynamicCast<RenderX>` 只能发生在 compatible modifier 实现内；core 侧禁止 concrete cast。
- **生命周期**：accessor 是非拥有型裸指针，必须与持有 `RefPtr<Component>` / `RefPtr<RenderNode>` 的 holder 同时使用；不得单独缓存、跨异步任务或在 holder 销毁后继续使用。
- **语义保持**：原代码在使用时读取的可变属性，accessor 也必须在同一调用时机读取；禁止为减少边界接口而改为初始化快照。
- **职责拆分**：当 Component / Render / Element 生命周期不同时，必须拆分 accessor；不要建立包罗所有 concrete 对象的“万能 accessor”。
- **发布约束**：使用 accessor 的两个 .so 必须同步构建与发布；若未来引入独立替换、版本兼容或卸载需求，必须回退到显式 modifier 或引入带版本的边界协议。
- **计划留痕**：组件拆分计划必须说明为何不采用标准 modifier 平铺、accessor 分组、持有对象和调用时机。

---

## 4. 单组件迁移标准流程（8 步，以 chart = 提交 46648ae 为模板）

> 模板目录：[frameworks/compatible/components/chart/](../frameworks/compatible/components/chart)

### 步骤 A — 文件整体搬迁（git mv）
- `frameworks/core/components/<x>/*` → `frameworks/compatible/components/<x>/*`
- 若 DOM 类在 `frameworks/bridge/common/dom/dom_<x>.{h,cpp}`，一并搬入 `frameworks/compatible/components/<x>/dom_<x>.{h,cpp}`。
- 若组件有专属 `frameworks/core/components/declaration/<x>/<x>_declaration.{h,cpp}`，一并搬入 compatible；不要让 migrated Declaration 留在 core 静态创建表。
- 若组件已完成 components_ng 组件化，但旧管线 `<x>_model_impl.{h,cpp}` 仍在 core，按 3.5 与老框架 concrete 实现一并迁入 compatible。

### 步骤 B — Include 路径重写 + 头文件守卫改名
- `#include "core/components/<x>/<f>.h"` → `#include "compatible/components/<x>/<f>.h"`
- 守卫宏 `FOUNDATION_ACE_FRAMEWORKS_CORE_COMPONENTS_<X>_*` → `..._COMPATIBLE_COMPONENTS_<X>_*`
- **保留**对公共基础类型的引用（`core/pipeline/base/render_node.h`、`core/components/common/properties/color.h` 等）——compatible 仍依赖 core 的公共与服务能力。

### 步骤 C — 新增三类产物
1. **`<x>_loader.{h,cpp}`**：`ComponentLoader` 子类，实现 `CreateDomNode/CreateModel/CreateDeclaration/...`。有旧管线 ModelImpl 时，`CreateModel()` 返回 compatible 中的新实例；有专属 Declaration 时实现 `CreateDeclaration()`。
2. **`<x>_modifier_compatible.h` + `<x>_modifier.cpp`**：仅当核心侧需调用公共 Model/DOM 接口之外的组件特有方法时定义函数指针结构体，并通过 `GetCustomModifier()` 暴露。无特有方法需求时省略，禁止与 `CreateModel()` 重复承载同一组 Model 操作。
3. **`<x>_accessor.h` / `<render_x>_accessor.h`（可选）**：仅当满足 3.7 的高接口密度场景时新增，放在 core 公共层；compatible concrete 类实现该抽象，小型 modifier 仅提供 `getAccessor` / `getRenderAccessor` 等发现入口。

### 步骤 D — 构建接线（2 改 1 删）
- [frameworks/compatible/BUILD.gn](../frameworks/compatible/BUILD.gn)：把新/搬迁的 `components/<x>/*.cpp` 全部加入 `ace_compatible_source` 模板的 `sources`。
- [frameworks/core/BUILD.gn](../frameworks/core/BUILD.gn)：**删除** `"$ace_root/frameworks/core/components/<x>:ace_core_components_<x>_$platform"` 依赖行。
- 已组件化场景：从 `frameworks/core/components_ng/pattern/<x>/BUILD.gn` 删除 `<x>_model_impl.cpp`，并加入 compatible BUILD。
- 若迁移了 declaration 源文件，同步从 [frameworks/core/BUILD.gn](../frameworks/core/BUILD.gn) 的 core sources 删除 `components/declaration/<x>/<x>_declaration.cpp`。
- 删除老目录的 `frameworks/core/components/<x>/BUILD.gn`。

### 步骤 E — 注册表清理
- 注册三件套不可漏：
  1. [component_loader.cpp](../frameworks/compatible/components/component_loader.cpp) 顶部 include 新 loader 头；
  2. 其 `sLoaderMap` 增加条目 `{ "<x>", [](){ return new <X>Loader(); } }`；
  3. 名称与 DOM 节点名一致（如 `"chart"`、`"list-item-group"`）。
- Declaration 清理不可漏：从 [declaration_creator_manager.cpp](../frameworks/core/components/declaration/common/declaration_creator_manager.cpp) 删除已迁组件专属 Declaration 的 include 和静态 `DeclarationCreator<T>` 条目，使其走 loader 的 `CreateDeclaration()`。
- `frameworks/bridge/common/dom/dom_type.{h,cpp}` 中 `<x>` 专属字符串常量：本地专用者下沉到使用处；**跨边界仍被 core 引用者**须把 `ACE_EXPORT` 升级为 `ACE_FORCE_EXPORT`，否则进不了导出表。

### 步骤 F — 符号导出（`build/libace.map`）
当前为主库符号直接导出方式：搬迁代码对 core 的**每一处符号引用，禁止使用通配写法**，须补进 linker version script，否则兼容库加载失败。逐项核对被引用的：
- 函数（如 `ColorFactory::GetInstance`）
- RTTI（`vtable?for?...`、`VTT?for?...`）
- 数据符号（`DOM_*` 常量）

要求：每项须为迁移代码**真实引用且必要**，避免冗余导出；新增项在提交说明列明。

### 步骤 G — 反向解耦
核心侧原直接引用组件类型处（如 [js_command.cpp](../frameworks/bridge/js_frontend/js_command.cpp) 中的 `DynamicCast<DOMChart>` + 直调 setter），改写为经 `GetCustomModifier()` 取函数指针结构体回调，并缓存首次获取的指针。

若核心调用点是已组件化组件的 dynamic modifier，且公共 `<X>Model` 已覆盖旧管线能力，则不走上述 custom modifier：按 3.5 改为 `GetLoaderByName("<x>") → CreateModel() → <X>Model*`。两种反向解耦模式按接口覆盖范围二选一，不重复叠加。

若组件符合 3.7 的高接口密度条件，可使用 modifier + accessor：核心侧仍通过 `GetCustomModifier()` 获取边界表，由 `getAccessor(component)` 在 compatible 内完成 concrete cast；后续调用经 holder 保活后的 accessor 执行。accessor 与标准 modifier 是按接口密度选择的两种边界表达，不得再额外建立一套值快照 bindings 重复承载同一能力。

**同 SO 借道 helper 的收口**：若本次搬迁使得某个 core 侧 `<X>CompatibleModifierHelper` 从此**再无跨 SO caller**（compatible 侧原调用点均已改直连），须按 3.6.3 判定流程回退 helper 此前为该迁移新增的 `ACE_FORCE_EXPORT` 与 `libace.map` 条目；`nm -C` 应看到符号从 `T` 降为 `t`。

### 步骤 H — 依赖裁剪
清理因搬迁而失效的 include / `BUILD.gn` deps（如 `frameworks/bridge/common/BUILD.gn`、相关 `cj_*_ffi.cpp`）。

---

## 5. 迁移顺序（依赖分析与执行计划）

迁移顺序由**组件依赖关系**驱动。本节基于既有依赖分析（`split_old_components.md` 组件依赖图 + `parse_deps.py` 拓扑排序），结合当前实际迁移进度，给出顺序原则、合理性评估、已完成统计与剩余建议顺序。

### 5.1 顺序确定原则
- **被依赖者后迁（dependents-first）**：若 A 依赖 B，先迁 A、后迁 B，确保 core 不出现"反向依赖 compatible"（core 模块依赖 compatible 符号），避免破坏 dlopen 单向边界。
- **环依赖成组处理**：`Stack ↔ Picker`、`Stack ↔ Dialog ↔ CustomDialog` 存在循环依赖，无法干净拆分，须成组迁移或整组暂留 core。
- **基础组件垫底**：Text、Image、Box、Flex、Display、Padding、Scroll 等被大量组件依赖，作为最底层最后迁移。
- **与当前实现方式的关系**：在"主库符号直接导出"方式下，上述顺序为**软约束**而非硬门禁——可先迁依赖方，由 core 经 `libace.map` 导出被依赖方符号满足引用，代价是 map 导出项增加。严格遵循 dependents-first 的意义在于**最小化跨边界导出与环处理成本**。

### 5.2 依赖分析与拓扑排序
**来源**：`split_old_components.md`（组件级依赖图，排除 Text/Image/Flex/Box/Display/Padding 等基础依赖后）经 `parse_deps.py` 拓扑排序，得到串行顺序与 4 个并行波次；并识别出循环依赖组与"无法执行"（基础 + 环）组件集。

**合理性评估**：
- ✅ 方法正确：依赖图 + 拓扑排序是确定迁移顺序的正道；dependents-first 原则与单向 dlopen 边界一致。
- ✅ 环检测与并行波次规划实用，可作为批次执行参考。
- ⚠️ 组件级依赖 ≠ 符号级耦合：真实导出成本由实际符号引用决定，非粗粒度组件依赖。两个"有依赖"的组件可能只共享少量符号，反之亦然。
- ⚠️ "无法执行"措辞过于绝对：基础组件并非不可迁，而是最后迁（或留作公共层）；环组件可成组迁移。
- ℹ️ `components_ng` 与老框架是**两套独立并行流程**：每个组件在 NG 均有实现，但 NG 存在**不构成**老框架组件的废弃信号——老框架组件须**全部迁移**，与 NG 无关。定级只看耦合度与环依赖。
- ⚠️ 命名/拼写需规范化（PikcerView、ImageAnimatior、Transfrom、Dislplay；Div/ForeachV2 等在本代码库无对应独立目录）。

### 5.3 已完成整改统计
按"core 目录已清除、仅余 theme，或仅余 theme + 确有消费者的 `<x>_compatible_modifier_helper`"判定为已完成。对照建议顺序逐项核实（2026-07-11 基线，DataPanel 于 2026-07-17 更新）：

**已完成（core 已清或仅余 theme，18）**：Canvas、Clock、Label、Input、Picker（含 PickerDialog）、GridContainer、Svg、Chart、GridRow、Switch、PickerView、GridColumn、NavigationMenu、Piece、ImageAnimator、Video、ToolBarItem、DataPanel

**已完成（稳态：core 仅余 theme + helper，12 目录）**：Stepper、Badge、TabBar（含 TabContent/Tabs）、TextField（含 TextArea/Textfield）、QRCode、Search、Marquee、List（含 ListItem/ListItemGroup）、Refresh、Swiper（含 Swipe）、Rating、Toggle

**进度汇总**：

| 类别 | 数量 | 说明 |
|---|---|---|
| 已完成 | 30 目录 | core 已清、仅余 theme，或达 theme + helper 稳态 |
| 剩余（未启动） | ~32 | 见 5.4 |
| 阻塞（基础 + 环） | 31 | 见 5.4，最后处理 |

> 实际执行并未严格遵循建议拓扑序（如 Chart 早于 DataPanel/Progress 完成，迁移当时由 core 导出符号满足；DataPanel 已于 2026-07-17 完成），印证 5.1 所述"直接导出方式下顺序为软约束"。

### 5.4 剩余建议顺序
**剩余组件（按建议拓扑序，已剔除已完成与已删除项）**：
Popup → RichText → CustomPaint → IfElse → Texture → Tween → IndexerList → Progress → Div → ForeachV2 → Page → Web → Transform → Multimodal → Slider → Align → Stage → ToolBar → Calendar → Font → Panel → Wrap → Grid → Track → Tip → WatchSlider → FlexItem → FocusCollaboration → Display → DragBar → GridItem

> 其中 CustomPaint、Texture、Div 等在本代码库无独立目录（见下文"剔除"），非独立迁移单元。

**阻塞组件（基础 + 环，31，最后处理）**：
- 基础 / 共享：Box、Flex、Display、Padding、Scroll、Text、Image、Span、TextOverlay、Bubble、Button、Checkable、Clip、DropFilter、FocusAnimation、GestureListener、TouchListener、Triangle、Option、Select、SelectPopup、Menu、NavigationBar、Positioned、TabsV2
- 环依赖组：Stack、Picker、Dialog、CustomDialog、DialogTween

**处理建议**：
- 阻塞组件是 map 导出符号的主要来源（被大量组件依赖）。在"主库符号直接导出"方式下迁移最晚、收益最大也最难，建议作为独立批次评估。
- 环依赖组（Stack / Picker / Dialog）须成组迁移，不可单独拆。
- 迁移顺序依据耦合度（被 include 数）分波，详见 5.5。

### 5.5 逐组件拆分计划（实测定级）
基于每个组件实测数据逐一定级：① core 目录构成；② **耦合度**=core 其它目录 `#include` 该组件头文件的文件数（header include，真实依赖，无子串噪声）；③ 环依赖；④ compatible 现状。已完成 29 目录（见 5.3）不在此列。

> 前提：`components_ng` 与老框架是**两套独立并行流程**，每个组件在 NG 均有实现，但 NG 存在**不构成**老框架组件的废弃信号。**所有老框架组件均须迁移**；定级只看耦合度与环依赖，不看 NG。
> 耦合度即"被依赖广度"：高耦合基础件是被依赖方，按 5.1 的 dependents-first 原则**最后迁**，也是当前 map 体积主因。

**P1-A 第一批（叶子，耦合 0–5，19 个，环成员除外）**
RichText(0)、Grid(0)、Multimodal(0)、WatchSlider(0)、ToolBar(1)、Tip(1)、Triangle(1)、IfElse(2)、ForeachV2(2)、Align(2)、Bubble(2)、DropFilter(2)、GestureListener(2)、Wrap(3)、Panel(5)、FocusCollaboration(5)、Menu(5)、Span(5)、TouchListener(5)

**P1-B 第二批（中耦合，6–15，16 个）**
Popup(10)、Tween(15)、IndexerList(8)、Page(14)、Stage(10)、Font(14)、Track(6)、DragBar(11)、Clip(11)、FocusAnimation(11)、NavigationBar(8)、Option(6)、Padding(14)、Positioned(10)、SelectPopup(10)、GridItem(8)

**P2 最后批次（高耦合基础件，>15，map 体积主因，15 个）**

| 组件 | 耦合 | 组件 | 耦合 |
|---|---|---|---|
| Text | 72 | Button | 33 |
| Box | 65 | TextOverlay | 33 |
| Scroll | 57 | Progress | 28 |
| Image | 41 | Checkable | 29 |
| Select | 40 | Calendar | 27 |
| Flex | 35 | Slider | 22 |
| FlexItem | 35 | Web | 45 |
| Display | 18 | | |

**P3 环依赖组（成组迁移，不可单独拆）**
`Stack ↔ Picker`、`Stack ↔ Dialog ↔ CustomDialog`：Stack(26)、Dialog(60)、CustomDialog(0)、DialogTween(2) 须成组迁移。Picker 虽已迁入 compatible，因环仍经 map 向 Stack/Dialog 导出符号。

**剔除（非独立迁移单元，不单列计划）**
- Camera：组件已删除（无 `CameraComponent` / `DOMCamera`），无需关注
- Texture：属 `xcomponent` / `video`（texture 已在 compatible/components/video）
- Div：本代码库无对应类（依赖图中的 `<div>` 已不存在）
- CustomPaint：属 `canvas`（已迁）
- TabsV2：无独立老框架目录

**关键结论**
- 所有老框架组件均须迁移（NG 是独立并行流程，非替代信号）；定级只看耦合度与环依赖。
- 耦合度即被依赖广度：高耦合基础件（Text 72、Box 65、Scroll 57、Image 41、Dialog 60）是被依赖方，按 dependents-first 最后迁，也是当前 map 体积主因。
- 环依赖组（Stack/Dialog/CustomDialog/DialogTween）必须成组；Picker 虽已迁，仍因环导出符号。
- Align/Padding/Font 等基础语义件测得耦合偏低，可能因被 inline 进多处而非 header include（耦合度被低估），迁移时需谨慎核实（推测，待验证）。

**执行建议**
1. P1-A/B 两波批量迁移（叶子→中耦合），沉淀自动化检查脚本。
2. P2 高耦合基础件最后批次（独立评估，map 影响最大）。
3. P3 环组一次性成组迁移。

---

## 6. 迁移检查清单（Checklist）

每个组件迁移须逐项确认：

**代码**
- [ ] 文件已从 `core/components/<x>` 搬至 `compatible/components/<x>`
- [ ] DOM 类（若有）已搬入 compatible
- [ ] 已组件化组件的旧管线 `<X>ModelImpl`（若有）已搬入 compatible，core 仅引用公共 `<X>Model`
- [ ] 专属 Declaration（若有）已搬入 compatible，且其默认 attr/style/event/method 常量不再依赖 core `DeclarationConstants`
- [ ] 所有 include 路径与守卫宏已改写
- [ ] 公共类型 include 保留正确（未误删 `core/pipeline/base`、`core/components/common`）

**工厂与注册**
- [ ] `<x>_loader.{h,cpp}` 已新增，虚函数实现完整
- [ ] 有旧管线 ModelImpl 时，loader `CreateModel()` 返回 compatible 实现，dynamic modifier 经 `GetLoaderByName("<x>")` 缓存公共 Model 指针
- [ ] `component_loader.cpp`：include + `sLoaderMap` 条目 + 名称正确
- [ ] `DeclarationCreatorManager` 已删除 migrated Declaration 的静态 include/creator；loader 已实现 `CreateDeclaration()`（若组件有专属 Declaration）
- [ ] 仅在公共 Model/DOM 接口无法覆盖时新增 `<x>_modifier_compatible.h` + `<x>_modifier.cpp`，未与 `CreateModel()` 重复转发
- [ ] 反向调用点已按覆盖范围选择公共 Model `CreateModel()`、标准 custom modifier 或 3.7 modifier + accessor，且 core 侧无跨边界 concrete cast
- [ ] 若使用 accessor：拆分计划已记录选型理由，accessor 只使用 core/共享类型，concrete cast 只在 compatible modifier 内
- [ ] 若使用 accessor：每次调用都有 `RefPtr<Component>` / `RefPtr<RenderNode>` holder 保活，裸 accessor 指针未单独缓存、未跨异步任务保存
- [ ] 若使用 accessor：原代码的可变状态仍在原使用时机读取，未改为初始化快照

**构建**
- [ ] `frameworks/compatible/BUILD.gn` 已加全部 sources
- [ ] `frameworks/core/BUILD.gn` 已删对应依赖行
- [ ] components_ng BUILD 已删除迁移后的旧管线 ModelImpl source，避免 core/compatible 重复编译
- [ ] 老 `core/components/<x>/BUILD.gn` 已删
- [ ] 失效 deps / include 已清理

**符号**
- [ ] `libace.map` 新增项已逐条核对，均为迁移代码真实引用且必要
- [ ] `<X>ModelImpl` concrete 符号在主 `.so` 为 0、components `.so` 大于 0
- [ ] 已删除 helper/custom modifier 对应的无效 map 导出已同步清理
- [ ] **无同 SO 借道 helper**：`grep -rn "<X>CompatibleModifierHelper::" frameworks/compatible/` 无命中；若有，按 3.6 改直连
- [ ] **helper 无跨 SO caller 后必回退导出**：本次迁移引发的 helper `ACE_FORCE_EXPORT` + `libace.map` 条目已按 3.6.3 复查并回退；`nm -C` 核实相关 helper 方法为 `t`（local）非 `T`（global）
- [ ] 新增项已在提交说明列明
- [ ] `dom_type` 常量按本地/跨边界正确处理（`ACE_EXPORT` vs `ACE_FORCE_EXPORT`）

**完成判据**
- [ ] 编译：`./build.sh --product-name rk3568 --build-target ace_engine` 通过
- [ ] **两条消费路径均验证通过**：① 类 Web DOM 场景；② 声明式 `IsCurrentUseNewPipeline()==false` 老管线场景
- [ ] `core/components/<x>/` 仅余 theme，或 theme + 确有消费者的反向桥（符合稳态形态）

---

## 7. 分阶段路线图

| 阶段 | 动作 | 产出 |
|---|---|---|
| **P0** | 固化本流程；选 1 个"易"档组件完整走通，沉淀检查脚本 | 迁移模板 + 自动化校验 |
| **P1** | "易"档组件批量推进 | core/components 瘦身 |
| **P2** | "中"档组件迁移，补全各组件带出的 map 导出项 | map 随迁移增量、项项可核 |
| **P3** | "难"档组件迁移 | 老框架隔离主体完成 |

> 导出符号收敛，以及面向外部或独立版本演进的系统性边界协议设计为独立后续专题，不在本路线图内；同构建内部 accessor 模式已纳入本方案。
> 阶段 KPI：迁移不破坏编译、兼容库加载无符号缺失、相关组件 DOM/JS 功能正常、`core/components/<x>/` 达到稳态形态。

---

## 8. 风险与红线（关联 CLAUDE.md §8）

- **`libace.map` 即 ABI 契约**：当前为主库符号直接导出方式，新增项须为迁移代码真实所需、逐条核对；后续任何收缩均属 ABI-risk，须"标记废弃 → 过渡版本 → 裁剪"三步，不得一次裁剪。
- **构建依赖增删**：`frameworks/core/BUILD.gn`、`frameworks/compatible/BUILD.gn`、`bundle.json` 的 deps 变更属"先确认"事项。
- **公共 API**：`interfaces/native`、`interfaces/napi` 下的签名/语义/错误码/结构体布局不得改。
- **生成文件**：`**/generated/` 下不得手改。
- **跨边界 concrete cast**：core 侧禁止 cast 到 compatible 具体类；已有公共 Model 抽象时走 `CreateModel()` + 公共虚接口，接口之外的少量特有调用走函数指针结构体，高接口密度场景可按 3.7 由 compatible modifier 完成 cast 后返回 core 公共 accessor。
- **accessor C++ ABI 前提**：accessor 只允许用于同次构建、同步发布、不独立卸载的 ace_engine 内部 .so；若边界变为可独立升级或外部接口，必须切换为显式函数指针协议。
- **accessor 状态与生命周期**：禁止用初始化快照替代原代码的实时读取语义；禁止将非拥有型 accessor 裸指针脱离 holder 或跨异步任务保存。
- **dlopen 延迟暴露**：`RTLD_LAZY` 可能延迟符号缺失暴露，须在真机 DOM 场景验证加载与功能。

---

## 9. 附录：关键文件索引

| 用途 | 路径 |
|---|---|
| 兼容库主构建目标 | [build/BUILD.gn](../build/BUILD.gn)（`libace_compatible` @142、`libace_compatible_components` @186） |
| Loader 抽象 | [frameworks/compatible/components/component_loader.h](../frameworks/compatible/components/component_loader.h) |
| Loader 注册表 + C 入口 | [frameworks/compatible/components/component_loader.cpp](../frameworks/compatible/components/component_loader.cpp) |
| 动态加载实现（OHOS） | [adapter/ohos/osal/dynamic_module_helper.cpp](../adapter/ohos/osal/dynamic_module_helper.cpp) |
| 动态加载实现（preview） | [adapter/preview/osal/dynamic_module_helper.cpp](../adapter/preview/osal/dynamic_module_helper.cpp) |
| 符号导出契约 | [build/libace.map](../build/libace.map) |
| 迁移模板（chart） | [frameworks/compatible/components/chart/](../frameworks/compatible/components/chart) |
| 反向调用样例 | [frameworks/bridge/js_frontend/js_command.cpp](../frameworks/bridge/js_frontend/js_command.cpp) |
| 已组件化 Model 动态加载参考（Rating） | [rating_dynamic_modifier.cpp](frameworks/core/components_ng/pattern/rating/bridge/rating_dynamic_modifier.cpp)、[rating_loader.cpp](frameworks/compatible/components/rating/rating_loader.cpp) |
| 已组件化 Model 动态加载参考（DataPanel） | [data_panel_dynamic_modifier.cpp](frameworks/core/components_ng/pattern/data_panel/bridge/data_panel_dynamic_modifier.cpp)、[data_panel_loader.cpp](frameworks/compatible/components/data_panel/data_panel_loader.cpp)、[data_panel_model_impl.cpp](frameworks/compatible/components/data_panel/data_panel_model_impl.cpp) |

---

### 变更记录
| 版本 | 日期 | 说明 |
|---|---|---|
| v1 | 2026-07-11 | 初版基准方案 |
| v1.1 | 2026-07-11 | 移除原第 5 章"边界契约"方向（当前按主库符号直接导出方式实施，边界接口化留作后续专题）；同步修订步骤 F、路线图、风险红线措辞，章节重新编号 |
| v1.2 | 2026-07-11 | 重写第 5 章：以 `split_old_components.md` 依赖图 + 拓扑排序替换原"易/中/难"推测分级；新增顺序原则、合理性评估、已完成统计（29 目录）、剩余建议顺序（~33）、阻塞组件清单（31） |
| v1.3 | 2026-07-11 | 新增 5.5 逐组件拆分计划：基于 core 目录构成 + header-include 耦合度 + NG 替代 + 环依赖实测定级 |
| v1.4 | 2026-07-11 | 纠正：`components_ng` 与老框架是两套独立并行流程，NG 存在不构成老框架组件废弃信号——所有老框架组件均须迁移。移除 5.5 的 NG 维度与"评估废弃"逻辑，改为纯耦合度 + 环依赖驱动分波（P1-A 叶子 / P1-B 中耦合 / P2 高耦合基础件最后迁 / P3 环组成组 / 剔除） |
| v1.5 | 2026-07-11 | 新增 1.4：明确 `core/components` 服务两条消费路径（类 Web DOMxx 入口 + 声明式 `IsCurrentUseNewPipeline()==false` 老管线），`useNewPipeline_` 默认 false；迁移须同时验证两条路径，并补入检查清单完成判据 |
| v1.6 | 2026-07-11 | Camera 组件已删除，从剩余建议顺序链与剔除清单中移除/标注 |
| v1.7 | 2026-07-12 | 新增 Declaration 动态创建模式：`DeclarationCreatorManager` 静态表未命中后经 `DynamicModuleHelper` 调 loader `CreateDeclaration()`；迁移组件专属 Declaration 及其默认常量应下沉到 compatible，core 静态表需清理 |
| v1.9 | 2026-07-18 | 新增 3.6 同 SO 直连 vs 跨 SO 走 helper 模式：级联迁移后，若 compat 侧 caller 仍借道 core 侧 helper 会导致同 SO 内绕出去再回来 + 不必要的 `ACE_FORCE_EXPORT` / `libace.map` 导出；应改直连、回退导出，由链接器把 helper 符号从 `T` 降为 `t` 物理强制约束。参考实现 IndexerComponent 阶段 2 refactor。同步补入 §4 步骤 G 与 §6 检查清单符号栏 |
| v1.8 | 2026-07-17 | 新增“已组件化但老框架未隔离”的 ModelImpl 迁移模式：参考 Rating/DataPanel，以公共 Model + Loader `CreateModel()` 替代 core concrete ModelImpl 和重复 custom modifier/helper；补充决策规则、构建改线、禁止形态、符号验证及 Checklist |
| v1.10 | 2026-07-22 | 新增 3.7 高接口密度组件的 modifier + accessor 模式：明确同次构建/同步发布的内部 .so 可通过小型 modifier 完成能力发现与 concrete cast，由 core 公共 accessor 承载大量高内聚调用；补充选型规则、holder 生命周期、实时状态语义、发布前提、实施步骤与 Checklist |
