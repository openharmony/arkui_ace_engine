{
  "name": "@koalaui/ui-plugins",
  "main": "./lib/entry.js",
  "dependencies": {},
  "scripts": {
    "clean": "rimraf build lib .rollup.cache",
    "compile": "rollup -c ./rollup.config.mjs",
    "compile:libarkts": "npm run compile --prefix ../libarkts",
    "check": "npm run compile && rm -rf ../../arkoala-arkts/trivial/user/build && npm run build --prefix ../../arkoala-arkts/trivial/user",
    "check:run": "npm run check && npm run run  --prefix ../../arkoala-arkts/trivial/user"
  }
}
