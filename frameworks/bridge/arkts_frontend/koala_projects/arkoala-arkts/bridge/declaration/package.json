{
  "name": "@ohos/arkui",
  "version": "1.4.1",
  "description": "",
  "main": "./build/lib/src/index.js",
  "types": "./build/lib/src/index.d.ts",
  "scripts": {
    "clean": "rimraf build",
    "unmemoize": "ets-tsc -b ./tsconfig-unmemoize.json",
    "build": "npm run unmemoize && fast-arktsc --input-files ./arktsconfig-run-unmemoized.json --output-dir ./build --compiler ../../../incremental/tools/panda/arkts/arktsc --link-name bridge && ninja ${NINJA_OPTIONS} -f build/build.ninja",
    "compile": "ets-tsc -p ."
  }
}
